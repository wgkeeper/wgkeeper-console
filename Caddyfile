# WG Keeper Console — reverse proxy to app
# Set DOMAIN in .env (e.g. DOMAIN=console.example.com) for HTTPS with Let's Encrypt.
# Default localhost — only HTTP.
{$DOMAIN:localhost} {
    reverse_proxy app:8000
}
